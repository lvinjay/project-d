javascript:(async()=>{const clean=s=>String(s||"").replace(/\s+/g," ").trim();const num=s=>{const n=Number(String(s||"").replace(/[^\d.]/g,""));return Number.isFinite(n)?n:0};const params=new URLSearchParams(location.search);const category=params.get("pd_category")||params.get("query")||document.querySelector("h1")?.innerText||document.title||"네이버쇼핑";const minBudget=num(params.get("pd_min"));const maxBudget=num(params.get("pd_max"));const cards=[...document.querySelectorAll('a[href*="/products/"],a[href*="shopping.naver.com"]')];const results=cards.map(a=>{const root=a.closest("li")||a.closest("div");const text=clean(root?.innerText||a.innerText||"");const href=a.href||"";const img=root?.querySelector("img")||a.querySelector("img");const imageUrl=img?.src||img?.getAttribute("data-src")||"";const prices=[...text.matchAll(/(\d{1,3}(?:,\d{3})+)\s*원/g)].map(m=>num(m[1])).filter(v=>v>0);let price=prices.length?prices[0]:0;const sale=text.match(/(?:최대혜택가|최대할인가|할인가|판매가|최저)\D{0,30}(\d{1,3}(?:,\d{3})+)\s*원/i);if(sale)price=num(sale[1]);const reviewMatch=text.match(/(?:리뷰|상품평)\s*([\d,]+)/i);const ratingMatch=text.match(/([0-5]\.\d{1,2})/);let name="";const nameCandidates=root?[...root.querySelectorAll("a,strong,b")].map(el=>clean(el.innerText)).filter(v=>v.length>=5&&v.length<=180&&!/^[\d,]+\s*원$/.test(v)&&!/^새 창에서/.test(v)&&!/^찜하기/.test(v)&&!/^정상가/.test(v)&&!/^할인/.test(v)&&!/^배송/.test(v)):[];const productNames=nameCandidates.filter(v=>/로봇청소기|청소로봇|로봇 청소기/i.test(v));if(productNames.length)name=productNames.sort((x,y)=>y.length-x.length)[0];else if(nameCandidates.length)name=nameCandidates.sort((x,y)=>y.length-x.length)[0];return{name,text,url:href,imageUrl,price,reviewCount:reviewMatch?num(reviewMatch[1]):0,rating:ratingMatch?Number(ratingMatch[1]):0}}).filter(p=>p.name&&p.url&&p.price>0&&p.imageUrl);const unique=[];const seen=new Set();for(const p of results){const key=[p.name.toLowerCase().replace(/\s+/g,""),p.price].join("|");if(seen.has(key))continue;seen.add(key);unique.push(p)}const banned=/호환|소모품|부품|필터|먼지봉투|먼지백|물걸레\s*(패드|포|천|리필)|브러시|브러쉬|배터리|충전기|세정제|액세서리/i;const products=unique.filter(p=>!banned.test(p.name)).sort((a,b)=>(b.reviewCount-a.reviewCount)||(b.rating-a.rating)).slice(0,40);console.log("Project D 구조화 후보:",results.length);console.log("중복/액세서리 제거 후:",products.length);console.table(products.map((p,i)=>({순번:i+1,상품명:p.name,가격:p.price,리뷰:p.reviewCount,평점:p.rating})));if(!products.length){alert("수집 상품이 없습니다.");return}try{const response=await fetch("http://localhost:3000/api/naver-capture",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({category,minBudget,maxBudget,products})});const result=await response.json();if(!response.ok||!result.success)throw new Error(result.message||"전송 실패");alert(`Project D 후보 ${result.count}개 수집 완료\n예산: ${minBudget?minBudget.toLocaleString()+"원":"제한 없음"} ~ ${maxBudget?maxBudget.toLocaleString()+"원":"제한 없음"}`);window.open(`http://localhost:3000/admin/naver-capture?id=${encodeURIComponent(result.id)}`,"_blank")}catch(error){console.error(error);alert("Project D 전송 실패: "+(error?.message||error))}})()
